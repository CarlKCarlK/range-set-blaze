[target.wasm32-unknown-unknown]
runner = "wasm-bindgen-test-runner"
rustflags = ["--cfg=getrandom_backend=\"wasm_js\""]

[alias]
xtest = "nextest run -p range-set-blaze --lib --tests --examples"
check-all = "run -p xtask -- check-all"
